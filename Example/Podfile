use_frameworks!
platform :ios, '8.0'
target 'BMKit_Example' do
  pod 'BMKit', :path => '../'

  target 'BMKit_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.2.0'
    pod 'Nimble', '~> 7.0.2'
  end
end
